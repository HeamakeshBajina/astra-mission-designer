[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "astra-mission-designer"
version = "0.1.0"
description = "Computational aerospace platform for spacecraft trajectory analysis and mission optimization."
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]